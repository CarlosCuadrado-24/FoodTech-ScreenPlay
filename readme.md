# Ejecución de Tests

## Historia 1 (HU-QA-001)
```bash
.\gradlew.bat clean test --tests "runners.CreacionDePedidoRunner"
```

## Historia 2 (HU-QA-002)
```bash
.\gradlew.bat clean test --tests "runners.ModificacionDePedidoRunner"
```

## Historia 3 (HU-QA-003)
```bash
.\gradlew.bat clean test --tests "runners.ConsultarTareasPorEstacionRunner"
```
